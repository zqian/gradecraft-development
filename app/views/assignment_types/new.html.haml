.small-12.panel.mainContent
  / Assignment Type Navigation
  .show-for-medium-up
    = content_nav do
      = render "assignments/tabs"

  = content_nav_for AssignmentType, "New #{term_for :assignment_type}"

  %h3.pagetitle= @title

  .small-12.columns
    = render 'form'

  .clearfix
