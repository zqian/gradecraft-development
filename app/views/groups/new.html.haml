.small-12.panel.mainContent
  - if current_user_is_student?
    = render "students/student_profile_tabs"

  = content_nav_for Group, "New #{term_for :group}"

  %h3.pagetitle= @title

  .small-12.columns
    = render 'form'
