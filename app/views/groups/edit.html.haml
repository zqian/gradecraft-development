.small-12.panel.mainContent
  / Groups Navigation
  = content_nav do
    - if current_user_is_staff?
      = render "tabs"
    - else
      = render "students/student_profile_tabs"

  = content_nav_for @group, "Edit #{term_for :group}"

  %h3.pagetitle= @title

  .small-12.columns
    = render 'form'

  .clearfix
