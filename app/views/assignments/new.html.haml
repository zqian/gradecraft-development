.small-12.panel.mainContent
  / Assignments Navigation
  = content_nav do
    = render "tabs"

  = content_nav_for Assignment, "New #{term_for :assignment}"

  %h3.pagetitle= @title
  
  .small-12.columns
    = render 'form'

  .clearfix
