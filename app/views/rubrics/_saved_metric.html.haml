.row
  .small-8.columns(style="padding:0")
    %input.needs-data(type="text" ng-model="MetricName" placeholder="Name" value="{{metric.name}}")
  .small-4.columns(style="padding:0")
    %input.needs-data(type="text" ng-model="MetricMaxPoints" placeholder="Max Points" value="{{metric.max_points}}")
.row
  %input(type="text" ng-model="MetricDescription" placeholder="Description" value="{{metric.description}}")
  {{metric.tiers.length}}
%input(type="hidden" value="{{$index}}")
%button(type="button" ng-click="metric.addTier()") Add Tier
%button(type="button" ng-click="destroyMetric($index)") Delete Metric
