.row
  .small-8.columns(style="padding:0")
    %input(type="text" ng-model="tier.name" placeholder="Tier" value="{{tier.name}}" required)
    {{TierName}}
  .small-4.columns(style="padding:0")
    %input(type="number" ng-model="tier.max_points" placeholder="Point Value" value="{{tier.max_points}}" required min=0 max=100)
.row
  %input(type="text" ng-model="TierDescription" placeholder="Description" value="{{tier.description}}")
%button.delete(type="button" ng-click="metric.removeTier($index)") Delete
{{$index}}
