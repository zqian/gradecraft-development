.small-12.panel.mainContent
  / Edit user breadcrumbs
  = content_nav do
    = render "users/tabs"

  = content_nav_for @user, "Edit User"

  %h3.pagetitle= @title

  .small-12.columns
    / Form to edit user    
    = render 'form'

  .clearfix